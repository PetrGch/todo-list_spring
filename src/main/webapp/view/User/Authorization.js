import React, {Component} from 'react';

export default class Authorization extends Component {
    render() {
        return (
            <div>
                Authorization
            </div>
        );
    }
}